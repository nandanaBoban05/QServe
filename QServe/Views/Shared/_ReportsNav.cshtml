@{
    string currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}

<nav class="reports-nav">
    <a class="@(string.Equals(currentAction, "Sales", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="Sales">Daily Sales</a>
    <a class="@(string.Equals(currentAction, "PopularItems", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="PopularItems">Popular Items</a>
    <a class="@(string.Equals(currentAction, "Throughput", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="Throughput">Kitchen Throughput</a>
    <a class="@(string.Equals(currentAction, "PaymentLog", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="PaymentLog">Payment Log</a>
    <a class="@(string.Equals(currentAction, "OrderStatus", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="OrderStatus">Order Status Breakdown</a>
    <a class="@(string.Equals(currentAction, "TableUtilisation", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-controller="AdminReports" asp-action="TableUtilisation">Table Utilisation</a>
</nav>
